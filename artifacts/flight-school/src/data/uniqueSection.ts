// A page-specific prose block rendered prominently on landing / air-tour pages
// to give each URL genuinely unique body content beyond the shared template.
export interface UniqueSection {
  heading: string;
  paragraphs: string[];
}
